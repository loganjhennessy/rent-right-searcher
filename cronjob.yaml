apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: rent-right-searcher
spec:
  schedule: "*/5 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: rent-right-searcher
            image: gcr.io/rent-right-dev/rent-right-searcher:latest
            imagePullPolicy: Always
            volumeMounts:
            - name: config-volume
              mountPath: /opt/searcher/config
            - name: credentials-volume
              mountPath: /opt/searcher/creds
          volumes:
          - name: config-volume
            configMap:
              name: searcher-config
          - name: credentials-volume
            secret:
              secretName: searcher-secret
          restartPolicy: Never